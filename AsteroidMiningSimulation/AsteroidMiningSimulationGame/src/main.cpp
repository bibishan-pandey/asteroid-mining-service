#include <iostream>

#include "SFML/Graphics.hpp"

int main() {
    sf::RenderWindow window(sf::VideoMode({100, 100}), "Asteroid Mining Simulation");
    std::cout << "Hello, World!" << std::endl;
    return 0;
}
